import { BoardCategorySortPipe } from './board-category-sort.pipe';

describe('BoardCategorySortPipe', () => {
  it('create an instance', () => {
    const pipe = new BoardCategorySortPipe();
    expect(pipe).toBeTruthy();
  });
});
